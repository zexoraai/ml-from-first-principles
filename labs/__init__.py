"""Research labs: eight from-scratch implementations of foundational ML papers.

Each subpackage `pN_<name>` is self-contained and corresponds to one project in
`records/SCOPE.md`. Shared utilities are promoted into `labs.common` only once a *second*
project needs them (decision D-005: no speculative abstraction).
"""

__all__ = ["p1_transformer"]
